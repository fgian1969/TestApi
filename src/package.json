{
  "name": "firebasetestapi",
  "version": "1.0.0",
  "description": "ServerMiddleware on Firebase Hosting",
  "author": "Ferrari Gian Carlo <fgian1969@gmsail.com>",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "rm -rf ./.nuxt && nuxt build",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.3.2",
    "express-session": "^1.15.6",
    "nuxt": "^1.4.2"
  },
  "devDependencies": {
    "babel-eslint": "^8.2.1",
    "eslint": "^4.15.0",
    "eslint-friendly-formatter": "^3.0.0",
    "eslint-loader": "^1.7.1",
    "eslint-plugin-vue": "^4.0.0",
  }
}
